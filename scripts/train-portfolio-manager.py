

# action will be -1 to 1 across number of stocks
# observation will be current 22 feature stock data with labels removed and replaced with predictor values
# reward system will be how close it is to optimal policy?

# calcuate the optimal policy ? (how)
# train using an MSE loop (what models? all of them?)
# tranformer version?
